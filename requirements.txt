# Training, validation, and model export CLI/API for YOLOv8.
ultralytics

# ONNX model format, graph loading, and model validation.
onnx

# CUDA-enabled ONNX inference runtime used for the ONNX benchmark.
onnxruntime-gpu

# NGC 24.05 packages (RAPIDS, CuPy, Numba) require NumPy 1.x.
numpy==1.26.4

# Newer OpenCV wheels require NumPy 2.x, which conflicts with NGC 24.05.
opencv-python==4.10.0.84

# Reads image dimensions during VisDrone-to-YOLO annotation conversion.
Pillow
